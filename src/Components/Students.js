import React from "react";
import Diagram from "./Diagram";

const Average = (props) => {
	return (
		<div className="average">
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Evelyn = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Aranka = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Floris = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Hector = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Martina = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Maurits = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Rahima = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Sandra = (props) => {
	return (
		<div>
			{" "}
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Wietske = (props) => {
	return (
		<div>
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

const Storm = (props) => {
	return (
		<div>
			{" "}
			<Diagram
				toBeShownInChart={props.toBeShownInChart}
				filterGraph={props.filterGraph}
			/>
		</div>
	);
};

export {
	Average,
	Evelyn,
	Aranka,
	Floris,
	Hector,
	Martina,
	Maurits,
	Rahima,
	Sandra,
	Wietske,
	Storm
};
